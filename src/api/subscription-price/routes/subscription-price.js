'use strict';

/**
 * subscription-price router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-price.subscription-price');
